--- backend/tests/test_fees.py
+++ backend/tests/test_fees.py
@@ -39,7 +39,8 @@
     db_session.add(student2)
     db_session.commit()

-    # Reset overrides before each test
-    app.dependency_overrides = {}
+    # Clear auth overrides but preserve db override
+    if auth.get_current_user in app.dependency_overrides:
+        del app.dependency_overrides[auth.get_current_user]
     yield
